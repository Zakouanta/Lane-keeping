function A1 = A1matrix(m,Vx,cf,cr,lf,lr,Iz)

A1 = [0 1 0 0; 
    0 -((2*cf + 2*cr)/(m*Vx)) ((2*cf + 2*cr)/(m)) (((-2*cf*lf) + 2*cr*lr)/(m*Vx));
    0 0 0 1;
    0 -((2*cf*lf-2*cr*lr)/(Iz*Vx)) ((2*cf*lf-2*cr*lr)/inertia) -((2*cf*(lf^2)+(2*C_r*(l_r^2)))/(inertia*V))];

B = [0;(2*C_f)/v_mass; 0; (2*l_f*C_f)/inertia];

Bd = [0; ((-((2*C_f*l_f - 2*C_r*l_r)/(v_mass*V)))-V); 0; -((2*C_f*(l_f^2)+2*C_r*(l_r^2))/(inertia*V))];
end